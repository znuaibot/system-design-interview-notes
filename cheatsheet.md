# System Design Cheatsheet

## 45-Minute Flow
| 时间 | 目标 | 完成标准 |
|---|---|---|
| 0–5m | 澄清范围 | 3–5 个核心用例 + SLO + out-of-scope |
| 5–10m | 粗略估算 | 峰值 QPS、存储、带宽、主导成本 |
| 10–20m | 高层设计 | API、数据模型、读写主路径得到共识 |
| 20–38m | 深挖 | 2 个关键瓶颈 + 故障/一致性语义 |
| 38–45m | 收尾 | 权衡、单点、扩展和可观测性 |

## Decision Rules
- 若状态可以重建且读多 → cache-aside；若不能重建 → 不把缓存当真相。
- 若请求跨网络 → 假设超时与重复；用幂等键和状态机，不假设“只调用一次”。
- 若要求顺序 → 先问“在哪个范围内”；优先按业务键映射到同一 partition。
- 若流量突发且任务可延迟 → 队列削峰；同时定义 backlog SLO、回压和死信。
- 若节点频繁变化 → 一致性哈希 + 虚拟节点；仍要单独处理 hot key。
- 若需要附近搜索 → cell 候选召回 + 邻格 + 精确距离过滤。
- 若是资金变化 → double-entry ledger + reconciliation；余额不是唯一审计来源。
- 若需要可重放状态 → durable log + deterministic consumer + snapshot。
- 若 celebrity 导致 fanout 爆炸 → 普通用户写扩散，名人读时合并。

## Estimation Defaults
| 项目 | 快速算法 |
|---|---|
| 平均 QPS | 每日请求数 / 86,400 |
| 峰值 QPS | 平均 QPS × 明确的峰值系数 |
| 存储 | 写入量 × 对象大小 × 保留期 × 复制/索引系数 |
| 带宽 | QPS × 平均 payload；分别算 ingress/egress |
| 缓存 | 热数据工作集 × 对象大小 + 元数据开销 |
| 分区 | max(容量需求/单节点容量, 吞吐需求/单节点吞吐)，再留余量 |

## Failure Questions
1. 权威状态在哪里，缓存/索引能否重建？
2. 在“数据库已提交但响应丢失”时，客户端重试会怎样？
3. 队列重复、乱序、积压和 poison message 怎么处理？
4. 分片扩容、热点与跨分片事务怎么办？
5. 单节点、AZ、区域故障的 RTO/RPO 是什么？
6. 用哪些指标检测延迟、错误、饱和度和数据不一致？

## Smells
- “使用微服务/NoSQL 所以可扩展” → 没有约束与数字。
- “保证 exactly-once” → 未说明边界、幂等与外部副作用。
- “加缓存解决” → 未定义失效、击穿和真相来源。
- “数据库分片” → 未给分片键、热点、迁移和跨分片查询。
- “多活” → 未说明冲突、路由、故障切换与脑裂。
